import 'package:flutter/material.dart';

class WareHouseBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child: Text("ini Warehouse"),
        )
      ],
    );
  }
}
